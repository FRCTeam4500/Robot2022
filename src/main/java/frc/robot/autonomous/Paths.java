package frc.robot.autonomous;

import edu.wpi.first.math.trajectory.Trajectory;


/**
 * Class for generating autonomous paths manually
 * We might not actually need this, if pathweaver actually works
 */
public class Paths {
    public static Trajectory firstBall(){ //TODO: Put in the path waypoints taken from pathWeaver
        Trajectory path = new Trajectory();
        return path;
    }
    public static Trajectory secondBall(){ //TODO: Put in the path waypoints taken from pathWeaver
        Trajectory path = new Trajectory();
        return path;
    }
    public static Trajectory consoleOut(){ //TODO: Put in the path waypoints taken from pathWeaver
        Trajectory path = new Trajectory();
        return path;
    }
    public static Trajectory consoleBack(){ //TODO: Put in the path waypoints taken from pathWeaver
        Trajectory path = new Trajectory();
        return path;
    }
}
